interface GqlObject<T extends string = string> {
  __typename: T
}

export const isUser = (gqlObject: GqlObject): gqlObject is CurrentUserQuery_currentUser => {
  return gqlObject.__typename === 'User'
}
